board_manager:
  additional_urls:
    - https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json

directories:
  data: ./data
  downloads: ./downloads
  user: ./user
  builtin:
    libraries: ./builtin-libraries

sketchbook_path: ./sketchbook

library:
  enable_unsafe_install: true

daemon:
  port: "50051"

logging:
  level: info
  format: text